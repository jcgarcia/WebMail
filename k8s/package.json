{
  "name": "@webmail/k8s",
  "version": "0.1.0",
  "private": true,
  "description": "WebMail - k8s component",
  "main": "dist/index.js",
  "scripts": {
    "dev": "echo 'Add dev script for k8s'",
    "build": "echo 'Add build script for k8s'",
    "test": "echo 'Add test script for k8s'",
    "lint": "echo 'Add lint script for k8s'"
  }
}
