{
  "name": "webmail",
  "version": "0.1.0",
  "private": true,
  "description": "SnappyMail web mail client for ingasti",
  "scripts": {
    "dev": "pnpm --parallel run dev",
    "build": "pnpm --parallel run build",
    "test": "pnpm --parallel run test",
    "lint": "pnpm --parallel run lint"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
